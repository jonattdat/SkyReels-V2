# Dependencies for the SkyReels-V2 REST API server (api_server.py).
#
# These are lightweight and do NOT include the model runtime. For real GPU
# inference, also install the main requirements: `pip install -r requirements.txt`.
# For GPU-free frontend development, these alone are enough with SKYREELS_MOCK=1.
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
python-multipart>=0.0.9
pydantic>=2.5.0
imageio[ffmpeg]>=2.31.0
imageio-ffmpeg>=0.4.9
numpy>=1.23.5,<2
pillow>=10.0.0
